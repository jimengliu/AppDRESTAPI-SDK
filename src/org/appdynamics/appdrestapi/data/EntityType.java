package org.appdynamics.appdrestapi.data;

/**
 * An enumeration defining the entity type.
 *
 * This Enum must be kept in sync with the Enum "rest-api.proto.EntityLogLevel.EntityTypeEnum.EntityType"
 * at all times. The order of the enum values must be the same. Furthermore, for compatibility reasons the new values
 * added to this enum should be added at the end; it will guarantee that the ordering of dual enum in the protobuf remains unchanged.
 *
 * Full path for the protobuf definition file: APPDYNAMICS_HOME/codebase/agent-api/src/main/proto/rest-api.proto
 * After adding the new types to the "rest-api.proto" file run "gradle generateSources" in APPDYNAMICS_HOME/codebase/agent-api
 * directory and then commit the changed files.
 */
public enum EntityType {
    ACCOUNT,
    AGENT_CONFIGURATION,
    ALL,
    APPLICATION,
    APPLICATION_COMPONENT,
    APPLICATION_COMPONENT_NODE,
    APPLICATION_DIAGNOSTIC_DATA,
    MOBILE_APPLICATION,
    AUTOMATIC_LEAK_DETECTION,
    BACKEND,
    BACKEND_DISCOVERY_CONFIG,
    BUSINESS_TRANSACTION,
    BUSINESS_TRANSACTION_GROUP,
    CALL_GRAPH_CONFIGURATION,
    CUSTOM_EXIT_POINT_DEFINITION,
    CUSTOM_MATCH_POINT_DEFINITION,
    CUSTOM_MEMORY_STRUCTURE,
    DASHBOARD,
    ERROR,
    ERROR_CONFIGURATION,
    DOT_NET_ERROR_CONFIGURATION,
    PHP_ERROR_CONFIGURATION,
    NODEJS_ERROR_CONFIGURATION,
    PYTHON_ERROR_CONFIGURATION,
    EUM_CONFIGURATION,
    BLITZ_CONFIGURATION,
    EVENT,
    GLOBAL_CONFIGURATION,
    INCIDENT,
    JMX_CONFIG,
    MACHINE_INSTANCE,
    MEMORY_CONFIGURATION,
    NOTIFICATION_CONFIG,
    OBJECT_INSTANCE_TRACKING,
    POLICY, // a.k.a. HealthRule
    RULE,
    SERVICE_ENDPOINT_DEFINITION,
    SERVICE_ENDPOINT_DEFINITION_CONTAINER,
    SQL_DATA_GATHERER_CONFIG,
    STACK_TRACE,
    THREAD_TASK,
    TRANSACTION_MATCH_POINT_CONFIG,
    USER,
    GROUP,
    ACCOUNT_ROLE,
    WORKFLOW,
    WORKFLOW_EXCUTION,
    POJO_DATA_GATHERER_CONFIG,
    HTTP_REQUEST_DATA_GATHERER_CONFIG,
    BASE_PAGE,
    IFRAME,
    VIRTUAL_PAGE,
    AJAX_REQUEST,
    SYNTH_JOB_REF,
    MOBILE_NETWORK_REQUEST,
    SERVICE_END_POINT,
    INFO_POINT,
    METRIC_BASELINE,
    DB_SERVER,
    DOT_NET_AGENT_CONFIG,
    DOT_NET_MACHINE_CONFIG,
    ACTION,
    ACTION_SUPPRESSION,
    EVENT_REACTOR,
    REMOTE_SERVICES,
    HTTP_REQUEST_ACTION_PLAN_CONFIG,
    HTTP_REQUEST_ACTION_MEDIA_TYPE_CONFIG,
    MOBILE_DEVICE,
    HOTSPOT_DEFINITION,
    HOTSPOT_DEFINITION_CONTAINER,
    JMX_INSTANCE_NAME,
    CUSTOM_EMAIL_ACTION_PLAN_CONFIG,
    SERVICE_ENDPOINT_MATCH_CONFIG,
    METRIC,
    NETWORK_ELEMENT,
    RUBY_ERROR_CONFIGURATION,
    ANALYTICS_HIERARCHICAL_CONFIGURATION,
    ANALYTICS_DASHBOARD,
    DB_MONITOR_CONFIG,
    ANALYTICS_DASHBOARD_LEGACY
}
